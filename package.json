{
  "name": "curriculo-click",
  "version": "1.1.0",
  "description": "Sistema de currículo web estático e dinâmico com suporte a Netlify CMS",
  "scripts": {
    "build": "rm -rf public && mkdir public && cpx \"**/*\" public",
    "dev": "echo 'Desenvolvimento local: abra o index.html no navegador'"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0"
  }
}
